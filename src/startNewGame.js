export default function startNewGame(setModal, setLife, setNumero, setDisplayModal, setError, setStart){
    setModal(false);
    setLife(3);
    setNumero(0);
    setDisplayModal(0)
    setError(false);
    setStart(true);

}